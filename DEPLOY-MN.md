# TourHome AI — Интернэтэд гаргах бүрэн заавар

Кодыг ажиллаж байгаа вебсайт болгож, утсанд аппаар суулгах хүртэлх алхмууд.
Нийт хугацаа: **20–40 минут**. Программын мэдлэг шаардахгүй, зөвхөн заавраа дагана.

---

## 0. Бэлтгэл

Гурван үнэгүй бүртгэл хэрэгтэй:

| Юу | Хаана | Юунд |
|---|---|---|
| GitHub | github.com | Кодоо хадгалах |
| Supabase | supabase.com | Өгөгдлийн сан, нэвтрэлт, зураг хадгалах |
| Vercel | vercel.com | Вебсайтыг интернэтэд байршуулах |

Компьютерт **Node.js 20+** суусан байх ёстой → nodejs.org-оос LTS хувилбарыг татна.
Шалгах: терминал дээр `node -v` бичихэд `v20…` эсвэл түүнээс дээш гарах ёстой.

---

## 1. Supabase project үүсгэх

1. supabase.com → **New project**
2. Мэдээлэл:
   - **Name**: `tourhome-ai`
   - **Database Password**: хүчтэй нууц үг үүсгээд **хадгалж ав** (дараа хэрэг болно)
   - **Region**: `Northeast Asia (Tokyo)` эсвэл `Southeast Asia (Singapore)` — Монголд хамгийн ойр
3. **Create new project** → 1–2 минут хүлээнэ.

---

## 2. Өгөгдлийн сангийн бүтцийг үүсгэх

1. Зүүн талын цэснээс **SQL Editor** → **New query**
2. Төслийн `supabase/schema.sql` файлыг нээж, **бүх агуулгыг хуулаад** тавина
3. **Run** дарна → `Success. No rows returned` гарвал болсон

Энэ нэг алхмаар дараах бүхэн үүснэ:

- 6 хүснэгт: `users`, `properties`, `property_images`, `property_tours`, `leads`, `analytics`
- Аюулгүй байдлын дүрмүүд (RLS) — зуучлагч зөвхөн **өөрийн** зараа удирдана
- 4 storage bucket: зураг, панорама, видео, аватар
- Автомат триггерүүд (профайл үүсгэх, үзэлт тоолох)

> Дахин ажиллуулж болно — алдаа өгөхгүй.

**Шалгах:** **Table Editor** дээр 6 хүснэгт, **Storage** дээр 4 bucket харагдах ёстой.

---

## 3. Түлхүүрүүдээ авах

**Project Settings → API Keys**. Гурван зүйл хэрэгтэй:

| Нэр | Хаанаас | Тайлбар |
|---|---|---|
| Project URL | Settings → API | `https://xxxx.supabase.co` |
| Publishable key | API Keys | `sb_publishable_…` — олон нийтэд ил байж болно |
| Secret key | API Keys → Secret keys → Reveal | `sb_secret_…` — **хэзээ ч хэнд ч бүү өг** |

> Хэрэв таны project хуучин загвартай бол `anon` болон `service_role` гэсэн түлхүүр харагдана — тэдгээр нь мөн ажиллана. Supabase 2026 оны туршид хуучин түлхүүрүүдийг халж байгаа тул шинэ `sb_publishable_` / `sb_secret_` хосыг сонгосон нь дээр.

---

## 4. Нэвтрэлтийн тохиргоо

**Authentication → URL Configuration**:

- **Site URL**: `http://localhost:3000` (одоохондоо. 8-р алхам дээр өөрчилнө)
- **Redirect URLs** дээр нэмэх: `http://localhost:3000/auth/callback`

**Authentication → Providers → Email**: `Enable email provider` асаалттай эсэхийг шалга.

> Туршилтын үед хурдан байлгах бол **Confirm email**-ийг түр унтраа. Бодит хэрэглээнд заавал асаа.

---

## 5. Компьютер дээрээ ажиллуулж үзэх

Төслийн хавтас руу орж терминал нээгээд:

```bash
npm install
```

Дараа нь `.env.example`-ийг хуулж `.env.local` нэртэй болгоод дотор нь бөглөнө:

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxx
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxxxxxxx
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

```bash
npm run dev
```

`http://localhost:3000` нээгээд туршина:

1. Бүртгүүлэх → нэвтрэх
2. **Шинэ зар** → мэдээлэл бөглөх → хадгалах
3. **Зураг** таб → зураг оруулах
4. **360°** таб → equirectangular (2:1 харьцаатай) панорама оруулах
5. Баруун талын **Нийтлэх** → холбоос хуулж шинэ цонхонд нээх

Бүгд ажиллаж байвал дараагийн алхам руу.

---

## 6. Кодоо GitHub-д байршуулах

```bash
git init
git add .
git commit -m "TourHome AI"
```

github.com → **New repository** → нэр `tourhome-ai` → **Private** → Create.
Дараа нь GitHub-ийн үзүүлсэн 2 мөрийг хуулж ажиллуулна:

```bash
git remote add origin https://github.com/ХЭРЭГЛЭГЧ/tourhome-ai.git
git branch -M main
git push -u origin main
```

> `.env.local` нь `.gitignore`-т орсон тул түлхүүрүүд GitHub дээр гарахгүй. Заавал шалга.

---

## 7. Vercel дээр байршуулах

1. vercel.com → **Add New → Project** → GitHub-аа холбож `tourhome-ai` репог сонго
2. Framework автоматаар **Next.js** гэж танина — тохиргоо өөрчлөх шаардлагагүй
3. **Environment Variables** хэсэгт 4 хувьсагчийг нэмнэ:

| Name | Value |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://xxxx.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `sb_publishable_…` |
| `SUPABASE_SERVICE_ROLE_KEY` | `sb_secret_…` |
| `NEXT_PUBLIC_SITE_URL` | `https://tourhome-ai.vercel.app` |

4. **Deploy** → 1–3 минут.

> Vercel эхний удаа домэйнээ өгсний дараа `NEXT_PUBLIC_SITE_URL`-ээ жинхэнэ хаягаараа засаад **дахин deploy** хийнэ. Энэ хувьсагчаар QR код үүсдэг тул буруу байвал QR буруу хаяг руу заана.

---

## 8. Supabase-аа шинэ домэйнтэй холбох

**Authentication → URL Configuration** руу буцаж:

- **Site URL**: `https://tourhome-ai.vercel.app`
- **Redirect URLs** дээр нэмэх:
  - `https://tourhome-ai.vercel.app/auth/callback`
  - `https://*-tourhome-ai.vercel.app/auth/callback` (preview deploy-д зориулж)

Хадгалаад вебсайт дээрээ бүртгүүлж туршина.

---

## 9. Өөрийн домэйн залгах (сонголтоор)

1. Домэйн худалдаж авах (жишээ нь `tourhome.mn`)
2. Vercel → төсөл → **Settings → Domains → Add** → домэйноо бичнэ
3. Vercel-ийн үзүүлсэн DNS бичлэгийг домэйн бүртгэгчийнхээ хяналтын самбарт нэмнэ:
   - `A` бичлэг → Vercel-ийн IP, эсвэл
   - `CNAME` бичлэг → `cname.vercel-dns.com`
4. 10 минут – 24 цагийн дотор идэвхжинэ (SSL автоматаар)
5. Дараа нь **дахин** шинэчилнэ:
   - Vercel env: `NEXT_PUBLIC_SITE_URL=https://tourhome.mn` → redeploy
   - Supabase: Site URL болон Redirect URL-уудыг шинэ домэйнээр

---

## 10. Утасны апп болгох (PWA)

Төсөлд PWA дэмжлэг суулгасан тул **App Store / Google Play шаардлагагүй**.

**Android / Chrome:**
Хяналтын самбарын дээд талд **«Апп суулгах»** товч гарч ирнэ → дарна.
Эсвэл хөтчийн цэс → «Install app» / «Add to Home screen».

**iPhone / Safari:**
Хуваалцах товч (□↑) → **«Add to Home Screen»** → Add.

Суулгасны дараа:

- Нүүр дэлгэц дээр өөрийн иконтой болно
- Хөтчийн хаягийн мөргүй, жинхэнэ апп шиг нээгдэнэ
- Сүүлд үзсэн хуудас, панорамууд офлайн ажиллана
- Аппын иконыг удаан дарахад «Шинэ зар», «Хүсэлтүүд» гэсэн товчлол гарна

> PWA нь зөвхөн **https** дээр ажиллана. Локал `localhost` дээр ажиллах ба Vercel дээр автоматаар https болно.

---

## 10.5 Утсаараа шууд 360° буулгах

Аппын дотор өөрөө 360° зураг буулгах боломж суусан: **Зар засах → 360° таб → «Утсаараа 360° буулгах»**.

**Хэрхэн ажилладаг вэ:** утасны гироскоп камерын чиглэлийг мэдэж, буулгасан кадр бүрийг бөмбөрцөг дээр байрлуулна. Гараар холбох, тусгай апп шаардахгүй.

**Хэрэглэх заавар (зуучлагчид зориулав):**

1. Өрөөний **голд** зогс, утсаа **босоо** барь
2. «Эхлүүлэх» дар → камер болон хөдөлгөөний мэдрэгчийн зөвшөөрөл өг
3. Дэлгэц дээр цагаан тойргууд гарна — голын цэгээ тойрог руу чиглүүл
4. Тогтоод хэсэг барихад **өөрөө автоматаар** авна (тойрог ногоон болно)
5. Байрандаа зогсоод **биеэрээ** аажим эргэ. Гараа сунгаж хөдөлгөвөл оёдол зөрнө
6. Дээш, доош ч бас чиглүүл — 18 цэг бүгдийг авбал бүтэн бөмбөрцөг болно
7. Хамгийн багадаа 6 цэг авсны дараа **«Дуусгах»** идэвхжинэ

**Чухал шаардлагууд:**

| Шаардлага | Тайлбар |
|---|---|
| **https** заавал | Камер, гироскоп нь зөвхөн https дээр ажиллана → **Vercel дээр гаргасны дараа** л утсан дээр туршиж болно |
| Гар утас | Ширээний компьютерт гироскоп байхгүй тул товч ажиллахгүй |
| iPhone | Safari дээр «Motion & Orientation Access» зөвшөөрөл асууна → зөвшөөр. Settings → Safari → Motion & Orientation Access асаалттай байх ёстой |
| Гэрэлтүүлэг | Тогтмол гэрэлтэй өрөөнд буулга. Цонх руу шууд харвал цайрч болзошгүй |

### Чанарыг юу шийддэг вэ

Аппликейшн дараах зүйлсийг **автоматаар** хийнэ:

| Асуудал | Шийдэл |
|---|---|
| Гироскопын хазайлт → зураг хоёрдмол харагдах | Кадр бүрийг өмнөх зурагтай тулган 0.07° нарийвчлалтай залруулна |
| Автомат экспозиц → нэг тал гэрэлтэй, нөгөө нь бараан | Кадр бүрийн өнгийг давхцаж буй хэсэгт нь тааруулна. Мөн эхлэхэд экспозиц, цагаан тэнцвэр, фокусыг түгжинэ |
| Хөдөлгөөнөөс болсон бүдэг кадр | Хурцыг хэмжиж, бүдэг бол авахгүй — «Бүдэг байна» гэж сануулна |
| Мозайк, шатласан ирмэг | Bilinear түүвэрлэлт |
| Дээд/доод хэсгийн нүх | Ойролцоох өнгөөр зөөлөн бүрхэнэ |
| Чимээ шуугиан, бүдэг | Хадгалахын өмнө ирмэг хамгаалсан цэвэрлэгээ + хурцлалт |

**Хэрэглэгчид зөвлөх зүйл** (энэ хоёр нь чанарт хамгийн их нөлөөтэй):

1. **Байрандаа зогсоод биеэрээ эргэ.** Гараа сунгаж хөдөлгөвөл ойрын эд зүйл зөрнө — гироскоп зөвхөн эргэлтийг мэдэх ба шилжилтийг мэдэхгүй
2. **Тогтмол гэрэлтэй өрөөнд.** Цонх руу шууд харвал экспозиц үсэрнэ

**Камерын өнцөг тохируулга:** доод талын гулсуургаар 45–90° хооронд тохируулна. Хэрэв оёдол давхарлаж/зөрж байвал 60–70° орчимд туршиж үзээрэй (ихэнх утсанд ~65°).

### AI нягтруулалт (сонголтоор)

Үндсэн цэвэрлэгээ ямар ч татацгүй, шууд ажиллана. Үүнээс гадна **төхөөрөмж дээрээ** ажиллах AI нягтруулалт залгаж болно — зураг хаашаа ч илгээгдэхгүй.

1. Real-ESRGAN x2 маягийн ONNX модель олж аваад `public/models/realesrgan-x2.onnx` дотор тавина
2. Тохиргоонд нэмнэ:

```
NEXT_PUBLIC_SR_MODEL_URL=/models/realesrgan-x2.onnx
```

Тавимагц буулгаж дуусахад «AI нягтруулж байна…» гэсэн алхам нэмэгдэнэ. Утсан дээр 30–90 секунд болно. Модель заагаагүй бол энэ алхам бүрмөсөн алгасагдана.

> Энэ нь гироскоп дээр суурилсан тул мэргэжлийн 360° камерын чанарт хүрэхгүй — өрөөний булан дээр бага зэрэг зөрөө үлдэж болно. Хамгийн өндөр чанар хэрэгтэй бол Insta360 маягийн камер, эсвэл Google Street View аппаар буулгаад файлаар оруулж болно.

---

## 10.6 Багц, үнэгүй туршилт

### Дүрэм

| Төлөв | Юу хийж болох |
|---|---|
| **Үнэгүй туршилт** | **1 зар** нийтэлнэ. Тэр зараа хэдэн ч удаа нуух/дахин нийтлэх боломжтой |
| **Идэвхтэй багц** | Хязгааргүй зар нийтэлнэ |
| **Багц дууссан** | Хуучин зарууд **хэвээрээ ажиллана**, зөвхөн шинээр нийтлэх хаагдана |

Энэ дүрэм **өгөгдлийн санд** (`enforce_publish_quota` триггер) шалгагддаг тул хэрэглэгч API руу шууд хандаж тойрч гарах боломжгүй. Мөн `protect_billing_columns` триггер нь хэрэглэгч өөрөө өөртөө багц олгохоос сэргийлнэ.

### Үнэ

| Багц | Хугацаа | Үнэ | Сард | Тур |
|---|---|---|---|---|
| Starter | 1 сар | ₮69,999 | ₮69,999 | 5 |
| Professional ⭐ | 3 сар | ₮159,999 | ₮53,333 · 24% хэмнэлт | 20 |
| Business 💎 | 12 сар | ₮499,999 | ₮41,667 · 40% хэмнэлт | Хязгааргүй |
| Үнэгүй туршилт | 7 хоног | ₮0 | — | 1 |

«Тур» гэдэг нь **зэрэг нийтлэгдсэн** зарын тоо. Нийтлэлээс хасвал байр сул гарна — зар устахгүй.

Үнэ, багцыг өөрчлөх: `src/lib/constants.ts` → `PLANS`.
Банкны мэдээллээ солих: мөн тэнд → `BANK`.

### Хэрэв та schema.sql-ыг аль хэдийн ажиллуулсан бол

Хамгийн хялбар нь: **`supabase/schema.sql`-ыг бүхэлд нь дахин ажиллуулах**. Тэр нь 002–005 migration бүгдийг агуулсан, дахин ажиллуулахад аюулгүй (idempotent).

Тусад нь ажиллуулах бол дараалал: `002_billing.sql` → `003_wire_payments.sql` → `004_access_window.sql` → `005_plan_limits.sql`.

### wire.mn төлбөрийн систем холбох

Онлайн төлбөр [wire.mn](https://wire.mn) (Buildry) дээр суурилсан — банкны апп, цахим хэтэвч, QR нэг гарцаар.

**1. Бүртгүүлэх** — [app.wire.mn](https://app.wire.mn) → утас + ДАН (KYC, нэг удаа) → орлого хүлээн авах данс холбох. Бүртгэл, dashboard үнэгүй; шимтгэл зөвхөн амжилттай гүйлгээнээс (1%-иас).

**2. Test key авах** — dashboard → API keys → `sk_test_…`.

**3. Webhook endpoint үүсгэх** — Wire dashboard-аас, эсвэл:

```bash
curl https://api.wire.mn/v1/webhook_endpoints \
  -H "Authorization: Bearer sk_test_..." \
  -H "Idempotency-Key: th-wh-1" \
  -d url=https://ТАНАЙ-ДОМЕЙН/api/wire/webhook \
  -d "enabled_events[]=payment_intent.succeeded"
```

Хариунд ирэх `whsec_…` **зөвхөн нэг удаа** харагдана — хуулж ав.

**4. Environment variables** (`.env.local` болон Vercel дээр):

```
WIRE_API_KEY=sk_test_xxxxxxxx
WIRE_WEBHOOK_SECRET=whsec_xxxxxxxx
WIRE_OPERATORS=sandbox
```

**5. Migration ажиллуулах** — SQL Editor дээр `supabase/schema.sql`-ыг **бүхэлд нь дахин ажиллуул**. Тэр нь 002–005 бүх шинэчлэлтийг агуулсан бөгөөд дахин ажиллуулахад аюулгүй.

**6. Турших** — Хяналтын самбар → Багц → багц сонгох → `pay.wire.mn` руу шилжинэ → test mode-д sandbox оператороор төлнө → webhook ирээд багц автоматаар идэвхжинэ.

**7. Live болгох** — оператор идэвхжүүлэх хүсэлт батлагдсаны дараа:

```
WIRE_API_KEY=sk_live_xxxxxxxx     ← live key
WIRE_OPERATORS=                   ← хоосон = акаунтын бүх оператор
WIRE_WEBHOOK_SECRET=whsec_…       ← live endpoint дахин үүсгэж шинэ secret
```

Vercel дээр солиод **redeploy**.

> **Аюулгүй байдал:** багц зөвхөн webhook-оор идэвхждэг. Хэрэглэгч `success_url` руу гараар орлоо ч юу ч болохгүй. Webhook бүрийн HMAC-SHA256 гарын үсгийг шалгаж, дараа нь PaymentIntent-ийг API-аар дахин уншиж баталгаажуулна. Давхардсан event-ийг `webhook_events` хүснэгтээр таслана.

### Хугацаа дуусахад юу болох вэ

Хугацаа дуусахад нийтэлсэн турууд **түр зогсоно** — устахгүй, холбоос нь ч өөрчлөгдөхгүй.

| Төлөв | Хугацаа | Турууд |
|---|---|---|
| Үнэгүй туршилт | Зар нийтэлснээс **7 хоног** | Нээлттэй |
| Идэвхтэй багц | `plan_expires_at` хүртэл | Нээлттэй |
| Хугацаа дууссан | — | **Түр зогссон** |

Үзэгч холбоосыг нээвэл 404 биш, «Энэ тур түр зогссон байна» гэсэн эелдэг хуудас гарна. Зуучлагч багцаа сунгамагц **яг тэр холбоос, QR код** дахин ажиллана — дахин нийтлэх, шинэ QR хэвлэх шаардлагагүй.

Мөн энэ хугацаанд:

- Хайлтын системд индекслэгдэхгүй (`noindex`)
- Шинэ хүсэлт, үзэлтийн бүртгэл хийгдэхгүй
- Зуучлагчийн хяналтын самбарт «Түр зогссон» гэсэн тэмдэг гарна

Хамгаалалт нь өгөгдлийн сангийн RLS дээр (`has_access()`) хийгдсэн тул API руу шууд хандаад ч харах боломжгүй.

Туршилтын хугацааг өөрчлөх: `public.trial_window()` функц (SQL) **ба** `TRIAL_DAYS` (`src/lib/constants.ts`) хоёрыг зэрэг солино.

### Гэрэлтэй / харанхуй загвар

Толгой хэсгийн ☀️🌙🖥 товчоор солино. Анхдагчаар **системийн тохиргоог** дагана (Windows/iOS/Android-ийн dark mode). Сонголт хөтөч дээр хадгалагдана, хуудас ачаалахад анивчихгүй.

### Төлбөр баталгаажуулах (гараар, нөөц арга)### Төлбөр баталгаажуулах (гараар, нөөц арга)

Зуучлагч **Хяналтын самбар → Багц** хэсгээс данс, гүйлгээний утгын код (`TH-XXXXXXXX`) харна. Мөнгө орсны дараа та SQL Editor дээр:

```sql
-- 3 сарын багц идэвхжүүлэх
select public.activate_plan('agent@example.mn', 'm3');
```

Буцаах утга нь багц дуусах огноо. Аль хэдийн багцтай хэрэглэгчид дуусах хугацаа дээр нь **нэмж** сунгана.

Төлөв харах:

```sql
select email, plan, plan_expires_at, trial_used
from public.users
order by created_at desc;
```

Гараар цуцлах:

```sql
update public.users set plan_expires_at = null, plan = 'trial'
where email = 'agent@example.mn';
```

> Хожим QPay/Stripe холбоход энэ `activate_plan` функцийг webhook-оос дуудахад л хангалттай.

---

## 11. Гарахаас өмнөх шалгах жагсаалт

- [ ] Бүртгүүлэх → и-мэйл баталгаажуулах → нэвтрэх ажиллаж байна
- [ ] Зар үүсгэх, засах, устгах ажиллаж байна
- [ ] Зураг, 360° панорама байршиж, харагдаж байна
- [ ] Нийтэлсэн зарын холбоос **нэвтрэхгүйгээр** нээгдэж байна
- [ ] Ноорог зар нь холбоосоор нээгдэхгүй байна (404) — RLS зөв ажиллаж буйн шинж
- [ ] QR код зөв домэйн руу заасан
- [ ] Хүсэлтийн форм илгээгдэж, хяналтын самбарт харагдаж байна
- [ ] Хоёр өөр бүртгэлээр орж, бие биенийхээ зарыг засаж чадахгүй байгааг шалгасан
- [ ] Утсан дээр PWA суусан
- [ ] Утсаараа 360° буулгаж үзсэн (https домэйн дээр)
- [ ] Эхний зар үнэгүй нийтлэгдэж, хоёр дахь нь хаагдаж байгааг шалгасан
- [ ] `activate_plan` ажиллаж, хязгаар алга болж байгааг шалгасан
- [ ] Хугацаа дуусахад тур «түр зогссон» болж, сунгахад дахин нээгдэж байгааг шалгасан
- [ ] Гэрэлтэй/харанхуй загвар зөв солигдож байна

---

## 12. Түгээмэл алдаа

| Шинж тэмдэг | Шалтгаан / Засвар |
|---|---|
| `Invalid API key` | Env хувьсагч буруу эсвэл Vercel дээр нэмээгүй. Нэмсний дараа **заавал redeploy** |
| Нэвтрэхэд `redirect_uri mismatch` | Supabase → Redirect URLs дээр `/auth/callback`-тай хаягаа нэмээгүй |
| И-мэйл ирэхгүй | Supabase-ийн үнэгүй и-мэйл нь цагийн хязгаартай. Бодит хэрэглээнд Resend/SendGrid SMTP холбоно (Authentication → Emails → SMTP) |
| Зураг харагдахгүй, `hostname not configured` | `next.config.ts` доторх `remotePatterns` таны Supabase домэйнтэй таарахгүй байна. `NEXT_PUBLIC_SUPABASE_URL` зөв эсэхийг шалгаад redeploy |
| Зураг оруулахад `403` / `new row violates row-level security` | Нэвтрээгүй байна, эсвэл `schema.sql` бүрэн ажиллаагүй. SQL-ээ дахин ажиллуул |
| QR буруу хаяг руу заасан | `NEXT_PUBLIC_SITE_URL` хуучин утгатай хэвээр. Засаад redeploy |
| 360° үзүүлэлт хоосон/хар | Панорама 2:1 харьцаатай equirectangular биш байна |
| Нийтэлсэн зар нээхэд **«Зар олдсонгүй»** | Migration дутуу — `schema.sql`-ыг бүхэлд нь дахин ажиллуул. Терминал/Vercel лог дээр `[tour] billing columns unavailable` гэж бичигдсэн байвал яг энэ шалтгаан |
| «Багцын тур хязгаарт хүрсэн» | Тухайн багцын зэрэг нийтлэх тоо дүүрсэн. Хуучин зараа нийтлэлээс хасах эсвэл багцаа ахиулна |
| Локал дээр PWA суухгүй | Хэвийн. `npm run build && npm start` дээр эсвэл https домэйн дээр л ажиллана |

---

## 13. Зардал

Эхлэхэд **бүрэн үнэгүй**:

- **Vercel Hobby** — хувийн болон туршилтын төсөлд үнэгүй
- **Supabase Free** — 500 MB өгөгдлийн сан, 1 GB файл хадгалалт, 5 GB урсгал, сард 50,000 хэрэглэгч

Анхаарах: үнэгүй Supabase project **1 долоо хоног хөдөлгөөнгүй байвал түр зогсдог** (дашбордоос дахин асаана), мөн үнэгүй багцад 2 идэвхтэй project байна.

Панорама зураг байрлал их иддэг тул (нэг панорама ~2–5 MB) 200–400 орчим панорама байршуулахад 1 GB дүүрнэ. Тэр үед Supabase Pro (сард ~$25) руу шилжинэ.

---

## 14. Дараагийн алхмууд

- **Тусгай и-мэйл** — Resend SMTP холбож `no-reply@tourhome.mn` хаягаас илгээх
- **Зураг шахалт** — байршуулахын өмнө browser дээр resize хийж зардал бууруулах
- **Домэйн бүр зуучлагчид** — `agent.tourhome.mn` маягийн дэд домэйн
- **Багийн эрх** — нэг байгууллагад олон зуучлагч
- **Hotspot засварлагч** — 360° дотор өрөө хоорондын холбоосыг гараар байрлуулах

---

## Түлхүүр файлууд

| Файл | Зориулалт |
|---|---|
| `supabase/schema.sql` | Өгөгдлийн сангийн бүх бүтэц — Supabase дээр нэг удаа ажиллуулна |
| `.env.example` | Хувьсагчийн загвар — хуулж `.env.local` болгоно |
| `README.md` | Техникийн архитектур, файлын бүтэц |
| `public/manifest.webmanifest` | PWA тохиргоо (аппын нэр, икон) |
| `public/sw.js` | Офлайн ажиллагаа (service worker) |
